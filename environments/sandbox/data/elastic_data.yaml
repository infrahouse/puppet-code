---
classes:
  - role::elastic_data

accounts::user_list:
  ubuntu:
    group: 'admin'

profile::elastic::backups::hour: '*/2'
profile::elastic::backups::minute: '0'

profile::unattended_upgrades::package_blacklist:
  - elasticsearch
