lookup_options:
  accounts::user_list:
    merge:
      strategy: deep

sudo::configs:
  'admin':
    'content'   : '%admin ALL=(ALL) NOPASSWD: ALL'
    'priority'  : 10

accounts::user_list:
  ubuntu:
    group: 'admin'
