---
classes:
  - role::elastic_data

accounts::user_list:
  ubuntu:
    group: 'admin'

profile::elastic::config::role: 'data, transform'

profile::unattended_upgrades::package_blacklist:
  - elasticsearch
